# PrecisionCalc MCP -- runtime dependencies (Python 3.11+)

# Core: official MCP Python SDK (FastMCP-style server + stdio/HTTP transports).
mcp>=1.2.0

# Date parsing + Easter computation for regional holidays.
python-dateutil>=2.9.0

# --- Recommended for production / full feature set ---
# HTTP transport (streamable-http) with API-key auth + rate limiting:
uvicorn>=0.30
starlette>=0.37
# Per-country holiday calendars (business_days accepts any ISO country code):
holidays>=0.50

# --- Optional (uncomment as needed) ---
# Distributed tracing:
# opentelemetry-sdk>=1.20
# opentelemetry-exporter-otlp-proto-http>=1.20
# Tests:
# pytest>=8.0
# hypothesis>=6.0
